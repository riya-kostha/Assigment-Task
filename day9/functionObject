
#include<iostream> 
#include<vector> 
#include<algorithm>
class Sum {
public:
	int a;
	Sum(int a) :a(a)
	{

	}

	void operator() (int val)
	{
		std::cout << val + a << " ";
	}
};

int main()
{	
	
	std::vector<int> v = { 1,2,3,4,5 };
	for_each(v.begin(), v.end(), Sum(10));
	
	return 0;
}
