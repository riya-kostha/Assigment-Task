#include<iostream>

int main()
{


	//control flow;
	/*int i = 1;
	{
		std::cout << "hello\n";
	}

	//for (i) // this line will give an error this not right syntax
		//std::cout << "hyy";
		
	int x = 2;
	if (x)
		std::cout << "yes";
	std::cout << "inside if";
	else
		std::cout << "inside else";		//this will give error bcs we are using two statement after is without {} and there is else also;
		*/

	if (sizeof(int) > -1)
	{
		std::cout << "yes";
	}
	else {
		std::cout << "no";
	}


	return 0;
}
